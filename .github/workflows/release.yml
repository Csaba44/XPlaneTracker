name: Build and Release EXE

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests pyinstaller rich SimConnect python-dotenv

      - name: Create .env file
        shell: bash
        run: |
          echo "DISCORD_WEBHOOK_URL=${{ secrets.DISCORD_WEBHOOK_URL }}" > XPlaneTracker/.env

      - name: Build EXE
        working-directory: ./XPlaneTracker
        run: |
          pyinstaller --onefile --name "CSABOLANTA" --add-data ".env;." --collect-all SimConnect main.py

      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          files: XPlaneTracker/dist/CSABOLANTA.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
