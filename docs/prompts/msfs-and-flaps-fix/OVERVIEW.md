# Task 1
First, confirm that the following is true:
Flap values retunr the following:
- MSFS: an index starting from zero, integer values, for every aircraft, including community made ones, paid and default
- X-Plane: an index float value between 0-1 - for every aircraft, including community made ones, paid and default

Confirm also, that when for example in a cessna, the flap lever is moved in the following way (cessnas have 10,20,30 deg positions so 3 in total):
- first one notch down to 10deg
- then down to 20 and immediately to 30 before its extended and locked on 20 - essentially skipping 20 all together, and moving from 10 to 30
we will have undefined behaviour in xplane (some value will be given between the positions), and in msfs only the first movement will be logged so it'll be logged first notch as 1, moving to 20 as 2 then the 30 will not be logged at all.
My prefered approach is when dealing with this, the skipped position should also be skipped in the log, and only show the final position. Maybe, before logging the flap, store the event data (timestamp, etc) and wait 5 seconds before logging to the file. If in the 5s timespan the lever moves again, it'll wait for the position to come out then overwrite the previous event then save it as flaps extended. If it does not move in 5s, the original will be saved, with the original timestamp. If you dont store it in a temp variable, it'll be possible that the map in the frontend shows 5s difference between the lever moving in actuality.

# Task 2
Regarding flaps, please come up with an idea how we will convert the index (1,2,3 in msfs and float values in xplane) to a common ground, working for all types of aircraft ranging from a cessna 172 to a 737 to an a320, across all simulator platforms seamlessly. While index 1 msfs is flaps 1 in a320, in the cessna it means flaps 10°. If you can not come up with a better one, we will crowdsource this - meaning that the user will be able to enter the index's corresponding flap value and it'll be saved to the database and from then on it'll be cached and displayed to the user as the crowdsourced one. Please also make sure after its displayed the crowdsourced information, it is still editable - in all cases, it should be shown on the admin page, so it cant contain false info - the admins need to approve it all the time. The user should be able to edit and send it to request approval on either the flight map by clicking on the icon, and in the tooltip - or in the timeline - the same way. If you do not have a genius level idea, or a dataset that contains indexes mapped by aircraft ICAO code in at least one of the simulators, we'll go with my idea, but that is not the best one so please get to work regarding this. If we go with crowdsourcing, we need to make sure if the flight associated with the flap event has an aircraft type - as it is an optional field - if not we will go with the bare indexes. If the associated flight and its aircraft icao is already stored in the database as a flap position, below the index we will show Flaps position: db entry. The entry should be string so the user can give 1+F and such values too, not just integers, because a cessna for example have 10° with the deg sign included. If only one of the simulators have a dataset mapping aircraft icao to flaps index, we will upload that as crowdsourcing, and people can still edit these and request approval, but they'll be the default values. As far as I know, neither sims have a dataref or a simvar that is telling us what is the flap setting positions name (at least none that are shared accross all developers' aircraft).

# Task 3
If the user shuts down the engine after landing (meaning the engine was previously on) send a windows notification to them, telling them they can upload the flight. Also only in xplane, if the user starts the replay mode, advise them with a warning that they still didnt upload the flight. If you decide a package is needed, as per @CLAUDE.md update the file: @.github/workflows/release.yml so the py app builds correctly.

# Task 4
When the user lands, a webhook event is sent to discord. Instead of displaying the full route, I want to display the departure airport's name (not icao) like Budapest Liszt Ferenc International Airport with a flag of that country as a discord emoji. Like this:
:flag_hu: Budapest Liszt Ferenc International Airport - :flag_hu: Debrecen International Airport
So the user knows where he has landed. The arrival icao is already displayed, so you just have to look that up via some api, i am sure you can find one inside this codebase too. Do not display arrival icao either, just the names like i said. If the name cannot be determined, please do not display anything regarding departure or arrival, just display the data that is available.

# Instructions
Please ask any questions you'd like to know, and create a plan and save it inside @docs/plans first, do NOT output it into the terminal. Only output the questions, and after asking questions, you should save it to plans, and after that you can tell me its done, I'll review give feedback and you can start implementing.